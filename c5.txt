Correct the record: attribute conversion was ALREADY implemented

I told Joel tuipet did not implement attribute conversion, and wrote that claim
into shop.py's docstring and test_item_info.py. It was wrong, and this undoes it.

I grepped for `FavAttribute` (empty on every row) and for code converting a pet's
attribute, found nothing, and concluded the mechanic was missing. But conversion
is not a `FavAttribute` mechanic at all -- it is encoded as the Va/Da/Vi DELTAS.
Board Game is Vaccine -15 / Data +15, and pet.py:4620 applies it through
applyAttributeChange + _compensate_attrs(), whose own comment names "the six +-15
TRADE toys (Board Game, Skateboard, Dumbbell...)" and conserves the total. Proved
it: a Va30/Da5/Vi5 pet using Board Game lands at Va15/Da20/Vi5 -- total unchanged.
So the authored descriptions do not lie; I did.

The lesson is the one this codebase keeps teaching: a grep for the name I EXPECTED
is not a search for the mechanic. Ask what the data does, not what it is called.

Nothing to build, then -- but the mechanic was ILLEGIBLE, which is probably why it
read as missing. The shop showed "Va-15 Da+15": accurate, and indistinguishable
from two coincidental nudges that happen to cancel. A symmetric zero-sum pair now
renders as the conversion it is: "Va->Da15". A uniform nudge still collapses to
"attr+1"; a LOPSIDED nudge (-15/+5) is neither, and is still spelled out rather
than dressed up as a trade.

We still generate the effect line instead of quoting the Description -- but for
the honest reason, now stated: a generated line carries the exact numbers and can
never drift from the data behind it. Prose goes stale; deltas do not.

1,258 tests green. test_item_info.py now pins that the trade is REAL: all six toys
are symmetric and zero-sum, using one actually converts and CONSERVES the total,
and a one-way boost (Vaccine Chip) is never dressed up as a trade.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
